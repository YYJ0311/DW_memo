```
기존 바닐라.js (순수 자바스크립트)
document.getElementBy~
                    (id, className, name)
    : 태그를 불러 옴

Jquery
$('.className').css('display','block')

자주 사용하는 jquery
    1. show, hide
    2. append
    3. children
    *****4. val, text, focus
    5. attr

프론트엔드 영역인지 백엔드 영역인지 회사마다 다르다..

해당 부모에 자식태그가 몇개인지 필요할 경우가 생김

바닐라에서 document.getElementBy~ 
=> 제이쿼리에서 $('#') 또는 $('.')

document.getElementByClass('name').style
    = $('.name').css()

$('.name').show() : 제이쿼리 show 함수를 클래스 name에서 호출
$('.name').hide()

show, hide, children, append, on
on('이벤트','함수(){}')
: 이벤트가 발생하면 함수를 실행하겠다

* 절차지향 프로그래밍
    C, 코볼

* 객체지향 프로그래밍
    자바, 파이썬, js, node, 리액트, C++, C#

this 본인
super 부모

```

```
문제풀기 

1. 메뉴 선택하면 mymenu로 이동
2. 주문버튼 누르면 선택한 메뉴와 수량 알람
3. 메뉴 선택 없이 주문 누르면 "메뉴 선택해주세요" 알람
```

```
플로우 차트 : 논리적인 쿼리를 그림으로 쉽게 나타낸 것(나중에 수정하기 쉬움) 
<--반대--> 스파게티 코드

```