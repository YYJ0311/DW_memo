```
controller의 결과를 받는게 웹이면 웹서버
                          안드로이드면 안드로이드서버
                          디바이스면 IoT서버

    => API를 배우고 있다.
    API서버를 자바로 배우고 있음
    전부 json으로 구현된다.

    웹은 json 또는 jsp(템플릿엔진)으로 데이터를 보낼 수 있다.
    지금은 @RestController을 사용해서 템플릿엔진 없이 데이터를 보냄
    

    API서버를 하나만 잘 만들면 웹/안드로이드/디바이스에 모두 적용할 수 있다.
```
```
Spring으로 구현한 API를 html에서 결과를 확인 
=> AJAX (컨트롤러에 요청을 보내서 json으로 결과를 받아 옴)
    핵심 : 비동기 통신(카페를 생각하면 됨)
    A(Asynchronous)JAX
```
```
Controller에 @CrossOrigin(origins = {"*"})을 써 줌
CrossOrigin : 보안 허용
```
```
1. jsp 문법 배우기
2. 사진 처리
3. 클라우드 올리기 (AWS)
4. 안드로이드
```