```
1. 수업 내용 정리, 게시판 다시 만들어보기
2. 코로나 정리하기
3. 프로젝트 주제 찾기
    2-1. 5/30 ~ 6/3 : 미니 프로젝트
        (6/7 ~ 6/10 은 수업 복습하기 바쁠 것)
        6/14 ~ 6/17 : 개인 미니프로젝트
        6/21 ~ 7/11 : 프로젝트
```
```
Private IP
    같은 네트워크에 있는 Private IP끼리는 Public IP 몰라도 통신 가능(Private IP로 연결함)
Public IP
    다른 네트워크에 있는 Private IP와 통신할 때 Public IP가 필요하다

    Public IP를 사용하기 위해서는
        1. 컴퓨터가 켜져 있어야 함
        2. 컴퓨터 내 서비스가 실행중이어야 함

    Server : 서비스 제공
    컴퓨터에 DB서버, 웹서버 등 여러 서비스가 가동중임. 우리가 원하는 서비스에 접속하기 위해 포트번호를 이용한다.
        (포트번호 : 서비스 식별 번호, 범위 : 0 ~ 65535)
        (포트번호를 바꾸려면 3000~9999 안에서 조정하는게 좋다. 다른 번호는 기본 서비스와 겹칠 우려 있음)
    윈도우에 서비스 검색해서 들어가면 지금 가동중인 서비스를 확인할 수 있음
```
```
이벤트 종류
    1. 키보드를 누를 때 : keydown
    2. 누른 키를 뗄 때 : keyup
    3. 누르고 있을 때 : keypress

    구글에 jquery spacebar key 처럼 검색해보기
```
```
자바스크립트 var, let, const
    1. var 
        변수 이름을 중복선언이 가능하다
        => 테스트에는 편리하지만, 코드가 많아지면 관리하기 힘들다.
    2. let과 const
        변수 이름 중복 불가능
        차이점
            let은 변수에 재입력이 가능하지만 const는 불가능하다.

            let name = "i'm let"
            let name = "i'm not let" => 콘솔로 확인하면 name이 이미 선언됐다고 나옴
            name = "i'm not let" => 이 때 비로소 name에 새로운 값이 입력된다(재할당됨)

            const name = "i'm const"
            const name = "i'm not const" => 콘솔로 확인하면 name이 이미 선언됐다고 나옴
            name = "i'm not const" => constant 변수 입력에러 출력
```