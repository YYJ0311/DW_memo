```
알아야 할 개념

    1. json
        json은 데이터형식
        (ex 사내 이메일 작성형식)

        json에서 key 입력하면 value 값 나오는 개념
        보안, 네트워크, 데이터 분석 분야, 채팅프로그램(카톡 등)과 Iot 에서 많이 사용

        var list = {
            name : "hyunsangwon",
            hobby : ["축구", "영화"],
            family : {
                number : "1",
                name : "아빠"
            }
        }



    Spring 종류
        Spring : spring boot와 MVC 패턴은 같지만 설정이 엄청 많음
        Spring egov
        Spring boot

    2. web.xml
        spring에서 사용
        spring은 메인함수 개념이 web.xml(여기서부터 실행함)
        
    3. 서블릿 컨테이너
        spring과 boot 차이점
            :8080 포트번호가 있다는 것 = 서버가 오픈되어 있다.
            웹을 만드려면 톰캣을 설치해야 함. 톰캣 = 웹 서버
            원래는 spring과 tomcat을 각각 설치해야 실행 됨
            spring boot는 tomcat이 내장됨
```
```
jsp 프로젝트 생성
    스프링 initializr 이용
    Maven
    그룹 : com.study
    artifact : jsp
    자바 버전 : 11
    dependencies  : spring web

    이클립스에서 import도 jsp로 하기
```
```
구글에 maven repository 검색, https://mvnrepository.com/
상단 검색창에 필요한 것 검색, 버전 선택해서 Maven/Gradle 코드 복붙해서 사용
```
```
gradle은 코딩 언어로, 파일 안에서 코딩 가능
maven은 아님

maven은 dependencies를 pom.xml에 입력함
    dependecies 태그 안에 적으면 된다.
    사이트에서 JSTL 검색, 제일 위에 있는 거 maven 태그 복붙
    maven은 refresh 안 해도 됨. 저장하면 끝
    version을 안 쓰면 최신버전 다운로드 받음

properties와 yaml은 구조만 다를 뿐 내용은 같음
```
```
src/main/java 에서 source folder 이름 src/main/webapp 생성
webapp 아래에 폴더 WEB-INF 생성
WEB-INF 아래에 폴더 views 생성
```
```
HTML = HTML + JS + CSS
JSP = HTML + JS + CSS + JAVA (Java 코드 사용 가능!)

Java Server Page

jstl
    jsp가 나오고 jstl이 나옴
    index.jsp에 java 소스가 너무 많아서 퍼블리셔가 어려움이 많았음 
        => jstl 출현 (jsp에서 java 코딩 X, jstl을 이용해서 java 코딩함)
```
```
JSP : 템플릿엔진
    return이 파일이름
    jsp를 외부로 빼면 실행 안 됨. jsp는 java 패키지 안에 있어야 한다.
    jsp 프로젝트는 파일 경로가 내부에 있다. (수업에서 이전까지 RestController로 만든 것들은 외부에 html 파일로 존재했음)
    jsp 안에서 ajax 사용 가능! but return을 파일이름으로 해야 되기 때문에 후처리가 필요함
```