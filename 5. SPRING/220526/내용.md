```
gps를 사용하는 데이터로 할게 많음
카카오 지도 api 활용

카카오 개발자 가입하기
```
```
DML (DBeaver, SQLyog, Mysql ==> auto commit 해제하고 사용)

컬럼을 추가, 수정, 삭제하고 싶을 때
    ALTER table board add column cnt integer(4) default 0;
                      modify ~

root랑 프로그래밍에서 최고 권한을 의미
    우리 계정은 root이기 때문에 모든 DB 접근이 가능함
    (root 권한을 가져서 DB 생성과 삭제, 테이블 생성과 삭제가 가능)

    신입이 올 떄마다 계정을 준다 
        ==> 대부분 DML(SELECT, UPDATE, INSERT, DELETE)만 가능한 계정
```
```sql
ALTER table board add column cnt integer(4) default 0;

-- root 계정이 계정을 생성함
-- '%' : 모든 IP 허용(모든 IP에서 사용 가능하게 만듦)
create user youngjoon@'%' identified by '123';

grant select,insert,update on dw.* to youngjoon@'%';
-- dw의 모든 테이블에 대하여 delete를 제외한 기능에 대한 권한 부여

-- edit connection으로 새 Username과 password 입력해서 로그인 한 뒤 부여된 권한을 다음처럼 확인함

select * from board
-- 데이터 선택 가능
ALTER table board add column x integer(4) default 0;
delete from board 
-- 위 두개는 권한이 없어서 사용 불가
```
```
우리가 집에서 학원의 개인 컴퓨터에 있는 DB에 접근하는 방법
    1. 학원의 컴퓨터를 켜놓은 상태로
    2. 집 컴퓨터에서 학원 public ip에 접근하고
    3. 학원의 공유기에 설정이 되어 있어야 한다.
        DB가 있는 컴퓨터에 연결될 수 있게 설정
```
```
개발 환경과 운영 환경을 나눠서 개발한다.
```