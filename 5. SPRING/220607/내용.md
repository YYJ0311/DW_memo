```
1. board 게시판에서 탈퇴한 students를 조회하고 싶은 경우
    board 조회하는 sqlmapper 쿼리에서 board를 왼쪽에 두고 left join 하면 된다.

2. 검색할 때 페이지기능 추가함.
    검색이 엔터 keyup일 때 이루어지는데, 검색하고 난 뒤 페이지를 이동하려고 하면 엔터를 눌렀다 떼기 전까지 넘어가지 않는다.
    그래서 메소드를 추가로 만들어서 페이지 넘어갈 때 추가한 메소드로 연결해줌
    => 이렇게 하면 같은 url을 2번 부르게 되는데 더 간결한 방법이 있을까?
    keyup 아래에서 ajax를 부르고 코드를 짜다보니 그 아래에서는 keyup이 최우선조건이 되고, ajax를 keyup 위로 올려서 쓰고 싶지만 그러면 조건을 못 쓰지 않나...??? 쓸 수 있을거 같기도 하고

    function 새로 만들어서 ajax 부르고 그 아래 조건 추가하는 방법으로 해보자.
```
```sql
학생 테이블의 pk인 학생번호가 게시판 테이블의 fk로 참조되고 있다.
그래서 게시글을 남긴* 학생을 삭제할 때 참조되고 있기 때문에 삭제되지 않았다.
=> fk를 제거하고 다시 fk를 지정하는 과정에서 조건을 추가하면 된다.

-- 1. board를 만들때 어떻게 만들었는지 확인함. board 테이블에 들어가서 foreign_key를 직접 확인해도 된다.
show create table board

-- 2. board 테이블의 FK 삭제
alter table board drop foreign key board_students_id_fk

-- 3. 새로 FK를 주면서 students 테이블에서 삭제될 때 board 테이블 값에 null을 주는 조건 추가
-- on delete cascade하면 같이 삭제됨
alter table board 
add foreign key (students_id) 
references students (students_id)
on delete set null
```
```
깃허브에 있는거 pull 받으려고 하니까 에러나서 git status로 확인하니 
    Your branch is based on 'spring-workspace/main', but the upstream is gone.
      (use "git branch --unset-upstream" to fixup)
라고 나옴.
git branch --unset-upstream 입력해주니 해결됨..

=> local branch가 spring-workspace/main에 있어야 하는데 해당 자리에 없어서 문제가 발생한 것
```