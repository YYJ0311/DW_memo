```
page handler 그래들에 추가함

    "hasPreviousPage": false,
    "hasNextPage": true,
    "navigatePages": 8,
    "navigatepageNums": [
        1,
        2
    ]

vsc에서 특정 문자 전부 바꾸기 : 드래그해서 하이라이트하고 ctrl+shift+h

ajax로 페이지 넘어가면 주소가 안 바뀌는 단점이 있음
    => 비동기 통신
    ***** => 뒤로가기하면 1페이지로 넘어감(주소가 안 남기 때문), 뒤로가기가 없다.
    ajax의 본래 목적은 사진, 동영상 업로드

    뒤로가기를 쓰고 싶으면 jsp, 리액트, 뷰를 사용해야 함

    장점) 페이지의 깜빡임이 없음

카카오개발자 검색하고 로그인
애플리케이션 -> 애플리케이션 추가하기
앱이름, 사업자명 입력하고 저장하고 클릭
플랫폼 - web - 사이트 도메인에 http://192.168.0.240:5501 입력함 (10개까지 저장 가능)

구글에 카카오맵 api 검색 - web - sample - 마커 생성하기 클릭
Javascript + HTML 선택
<script type="text/javascript" src="//dapi.kakao.com/v2/maps/sdk.js?appkey=발급받은 APP KEY를 사용하세요"></script>
카카오 개발자 - 앱 키 - javascript 키 복사해서 위 주소 사이에 입력
<div id="map" style="width:100%;height:350px;"></div> 로 지도 구역 만듦
그리고 마커 생성하기에 있는 나머지 코드들 넣으면 지도 생성완료!
```
```
주소를 요청할 때마다 ip를 출력해야 함
    컨트롤러의 모든 url에 다 추가해야하는가?
    => 사용자가 요청을 서버에 보낼 때 마다 중간에 요청을 가로챌 수 있음(인터셉터) - 스프링에서 지원
    => 가로채서 컨트롤러에 신호가 가기 전에 ip를 출력한다.

    interceptor 패키지, 클래스 생성
    HandlerInterceptor 상속받고 override 생성

@Component
    내가 직접 작성한 클래스를 스프링에게 빈(스프링이 관리하는 클래스)으로 등록하라는 뜻.
    <--> @Bean은 스프링이 만들고 스프링이 관리하는 클래스
    => new를 쓸 필요 없음(스프링이 대신함)

WebConfig 클래스
    WebMvConfigure을 상속받고 addInterceptors로 우리가 만든 인터셉터를 등록함

결과)
    접속한 IP : 0:0:0:0:0:0:0:1 ==> localhost를 의미함
```
```
ajax가 비동기이기 때문에 ajax를 실행시키고서 결과가 나오기도 전에 다음 행의 코드를 실행한다. 그래서 ajax 에서 추가한 html의 css를 ajax 아래에서 불러오는 경우, 아직 ajax가 다 실행되지 않고 css를 찾으려고 하므로 undefined로 나오게 된다.
    => ajax로 만든 html은 console.log()로 확인하기 어렵기 때문에 컨트롤이 힘들다

    동기였다면, html만드는 걸 모두 실행시킨 후 css를 불러올 수 있다.
```
```
사람인
프로그래머스
로켓펀치
```