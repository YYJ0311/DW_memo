```
데이터 내보내기 하면 컬럼의 제약조건(PK, auto increment 등)은 복사되지 않으니 따로 추가해야 함.
but 이렇게 생성할 필요 없이, 디비버에서 테이블의 DDL을 보면 MySQL 명령어가 나와있다.
이 명령어를 프로젝트 init.sql에 적고 테이블이 존재하지 않으면 생성하게끔 조건을 준다.
    CREATE TABLE IF NOT EXISTS `membership`
그럼 프로젝트가 실행될 때 테이블이 생성된다.
그리고 데이터가 있는 테이블에서 데이터 export(내보내기)를 통해서 생성된 테이블로 데이터를 이동시킨다.
```